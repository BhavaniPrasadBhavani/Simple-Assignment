[phases.setup]
nixPkgs = ['nodejs_18']

[phases.build]
cmds = [
  'cd backend',
  'npm ci',
  'npm run build'
]

[phases.start]
cmd = 'cd backend && node dist/main.js'

[variables]
NODE_ENV = 'production'
PORT = '3001'
